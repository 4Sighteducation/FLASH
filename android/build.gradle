// Root (project) build.gradle for RN 0.74.x + AGP 8.x

plugins {
  // This comes from @react-native/gradle-plugin (exposed via includeBuild in settings.gradle)
  id("com.facebook.react.rootproject")
  // (Optional) other root plugins can go here, but avoid legacy buildscript/classpath usage
}

// No legacy buildscript/classpath block is necessary.
// Repositories for plugin resolution are defined in settings.gradle -> pluginManagement.
// Module repositories remain inside each module's build.gradle (e.g. app/build.gradle).
